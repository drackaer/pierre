package cooking.database;

/**
 * @author sburton
 * Gives the source of the data, ie is it from a real DB, or a file, or just our test data
 * 
 * Modified by rmorris Oct 27, 2011
 * Since we didn't really do this kind of thing, changed it to be a flag for the inspiring set type
 */
public enum InspiringSetType {
	TEST,
	CHILI,
	FULL
}
